<h1>New order for <%= @order_type.title %></h1>
<%= form_for [@order_type, @order] do |f| %>

	<%= render 'error' %>
  
  <div class="field">
    <%= f.label :order %><br>
    <%= f.text_field :order, class: "form-control" %>
  </div>

  <div class="field">
    <%= f.label 'proofed by: User (model not defined)' %>
  </div>
    <table class="table">
      <tr class = "order-header">
      <th>Description</th>
      <th style="text-align:center">Status</th>
    </tr>
    <% @order_type.categories.each do |cat| %>
      <tr>
				<td colspan=2 style="background-color:#BAB9B9; padding-left: 1em">
					<strong><%= cat.name %></strong>
				</td>
      </tr>
        <% cat.tasks.each do |task| %>
      		<tr class="<%= cycle('line-even', 'line-odd') %>">
						<td style="text-align: left; padding-left: 1em">
							<%= f.label task.description %>
						</td>
      		<%= f.fields_for :validations do |builder| %>
						<td style="text-align: center">
							<%= builder.check_box :approval %>
							<%= builder.hidden_field(:task_id, :value => task.id) %>
						</td> 
      		<% end %>
     		 </tr>
   		 <% end %>
    <% end %>
    </table>
<div class="note-field">
    <h4><%= f.label :note, "Notes:" %><br><h4>
    <%= f.text_area :note, rows: 10, cols: 40, class: "form-control" %>
  </div>
  <div class="actions">
    <%= f.submit "Submit", class: "btn btn-primary" %>
  </div>
<% end %>

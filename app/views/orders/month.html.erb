<div id="orders">
	<h1 id="month"><%= @order_type.title %> for <%= @date.strftime("%B %Y") %> </h1>
  	<%= render 'calendar' %>

</div>

<div>
	<h1><%= render 'breakdown' %> </h1>	
</div>

<div id='chart'>
<%= render_chart @chart, 'chart' %>
</div>

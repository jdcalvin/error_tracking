<div id="orders">
	<h2 id="month"><%= @order_type.title %> for <%= @date.strftime("%B %Y") %> </h2>
  	<%= render 'calendar' %>
</div>

<div>
	<h1> Quality: <%= @order_type.no_errors.count %> / <%= @order_type.orders.count %> 
  | <%= @order_type.quality %>%</h1>
</div>

<div id='chart'>
<%= render_chart @chart, 'chart' %>
</div>
<%= include_gon %>
<div id="orders">
	<h2 style="padding-top:0">
		<%= @order_type.title %> for <%= @date.strftime("%B %Y") %> 
	</h2>
	<%= render 'calendar' %>
</div>

<div style="margin: 0; margin-top: 1em" class="panel panel-default" >
  <div class="panel-heading"> 
  	<%= render 'breakdown' %>
  </div>
		<div class="panel-body">
		<% if @orders.count > 0 %> 
			<div id="chart" class="chart"></div>
			<div id ="render-chart">
				
				<%= render 'bar_charts' %>
			</div>
			<%= render 'chart' %>
		<% end %>
		</div>
		
</div>